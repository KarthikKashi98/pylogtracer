# Full local/dev runtime: the package plus ALL providers and the agent.
# Dependencies are declared in pyproject.toml; the [all] extra pulls every
# provider (openai/anthropic/ollama) and langgraph.
#
# For a minimal core install instead, run:   pip install pylogtracer
# For just one provider:                      pip install pylogtracer[ollama]
-e .[all]
